{
  "name": "Credential",
  "type": "object",
  "properties": {
    "username": {
      "type": "string"
    },
    "password": {
      "type": "string",
      "description": "Primary password"
    },
    "extra_passwords": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Additional passwords to try for this username (multi-password strategy)."
    },
    "working_password": {
      "type": "string",
      "description": "The password that last worked (populated after a 'working' result)."
    },
    "site_key": {
      "type": "string",
      "description": "References Site.key"
    },
    "status": {
      "type": "string",
      "enum": [
        "untested",
        "working",
        "failed",
        "error",
        "disabled"
      ],
      "default": "untested"
    },
    "last_tested": {
      "type": "string",
      "format": "date-time"
    },
    "last_result_note": {
      "type": "string"
    },
    "attempts": {
      "type": "number",
      "default": 0
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "username",
    "password",
    "site_key"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}